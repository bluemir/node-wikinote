extends layout

block content
  div(class="alert")
    |주의 모든 파일및 하위 항목들이 이동합니다. 링크가 깨어질수도 있습니다.
    |링크가 깨어지는 것이 두렵다면 이 항목을 남겨두고 내용만 이동한다음
    |링크를 남겨두는 것을 고려하십시오
  input(type="text")
  button
    이동
